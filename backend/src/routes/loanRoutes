import express from "express";

import {
  createLoanRequest,
  checkBorrowLimit,
} from "../controllers/loanController.js";

const router = express.Router();

router.post("/", createLoanRequest);

router.get("/check/:user_id", checkBorrowLimit);

export default router;